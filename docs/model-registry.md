# Model registry

El registry mantiene versiones inmutables y exige activación explícita. Una
predicción puede identificar `model_name` y `model_version`, permitiendo
reproducir qué modelo estuvo activo sin sobrescribir versiones anteriores.
